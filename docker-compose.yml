version: '3.8'

services:
  mc-web-client:
    build: .
    container_name: mc-web-client
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - HOST=0.0.0.0
